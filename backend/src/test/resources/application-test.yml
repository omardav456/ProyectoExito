spring:
  datasource:
    url: jdbc:h2:mem:exito_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  flyway:
    enabled: false

app:
  security:
    enabled: false
  jwt:
    secret: test-secreto-seguro-para-perfil-test-exito-stock-ai-2026
    expiration-ms: 60000
  seed:
    catalog-enabled: false
    demo-enabled: false